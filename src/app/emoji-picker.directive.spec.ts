import { EmojiPickerDirective } from './emoji-picker.directive';

describe('EmojiPickerDirective', () => {
  it('should create an instance', () => {
    const directive = new EmojiPickerDirective();
    expect(directive).toBeTruthy();
  });
});
